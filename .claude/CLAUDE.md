In this repository focus on simplicity in implementation, security, modularity and adherence to good software engineering principles.

When a change is made, log it in the CHANGELOG.md file.

Commit after any feature implementation is successfully completed.

This is a security based application and may contain references to insecure commands. DO NOT run any insecure commands.

Any testing code that is created in this repository must never run dangerous code regardless of the failure status of the tested code including if the tested code is broken.